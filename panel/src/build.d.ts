// Constantes inyectadas por Vite en tiempo de build (vite.config.ts, bloque define).
// No existen en runtime como variables: el bundle lleva los literales ya sustituidos.
declare const __PANEL_VERSION__: string;
declare const __PANEL_COMMIT__: string;
declare const __PANEL_FECHA__: string;
